#include <bits/stdc++.h>
using namespace std;

int main() {
    int t;
    cin>>t;
    while(t--){
        int n;
        cin>>n;
        int s[n];
        for(int i=0;i<n;i++){
            cin>>s[i];
        }
        string v;
        cin>>v;
        int cnt = 0;
        set <int> test;
        for(int i = 0; i < v.length(); i++)
        {
            if(v[i] == '0')
            test.insert(s[i]);
        }
        cout << *test.begin() << endl;
        // your code goes here
    }

}
